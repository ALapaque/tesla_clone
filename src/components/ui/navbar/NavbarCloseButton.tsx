import styled from "styled-components";

const NavbarCloseButton = styled.img`
	align-self: flex-end;
	justify-self: flex-end;
	width: 40px;
  height: 40px;
	cursor: pointer;
`

export default NavbarCloseButton